# 🚀 Deployment Guide - Alley of Heroes

Полное руководство по развертыванию проекта "Алея Друзів" с использованием Docker.

## 📋 Содержание

- [Требования](#требования)
- [Быстрый старт](#быстрый-старт)
- [Конфигурация](#конфигурация)
- [Развертывание](#развертывание)
- [Cloudflare Setup](#cloudflare-setup)
- [Обслуживание](#обслуживание)
- [Устранение неполадок](#устранение-неполадок)

---

## 🔧 Требования

- **Docker** >= 20.10
- **Docker Compose** >= 2.0
- **Git**
- Минимум **2GB RAM** и **10GB** дискового пространства

### Установка Docker

#### Ubuntu/Debian:
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
```

#### macOS:
```bash
brew install --cask docker
```

---

## ⚡ Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd alleyofherrr
```

### 2. Запуск в одну команду

```bash
bash scripts/start.sh
```

Скрипт автоматически:
- ✅ Создаст `.env` файл из примера
- ✅ Соберет Docker образы
- ✅ Запустит контейнеры
- ✅ Инициализирует базу данных
- ✅ Создаст администратора

---

## ⚙️ Конфигурация

### Создание .env файла

```bash
cp env.example .env
```

### Обязательные параметры

Отредактируйте `.env`:

```bash
# 🔐 Безопасность
POSTGRES_PASSWORD=ваш_супер_безопасный_пароль
NEXTAUTH_SECRET=$(openssl rand -base64 32)
NEXTAUTH_URL=https://yourdomain.com

# 👤 Администратор
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=безопасный_пароль
ADMIN_NAME="Admin Name"
```

### Генерация секретов

```bash
# NextAuth Secret
openssl rand -base64 32

# Postgres Password
openssl rand -hex 20
```

---

## 🐳 Развертывание

### Вариант 1: Автоматический (Рекомендуется)

```bash
bash scripts/start.sh
```

### Вариант 2: Пошаговый

```bash
# 1. Остановить существующие контейнеры
docker-compose down

# 2. Собрать образы
docker-compose build --no-cache

# 3. Запустить контейнеры
docker-compose up -d

# 4. Инициализировать БД
bash scripts/init-db.sh
```

### Вариант 3: С Nginx (Production)

```bash
docker-compose --profile production up -d
```

---

## 🌐 Cloudflare Setup

### 1. Flexible SSL Mode

1. Перейдите в **Cloudflare Dashboard**
2. Выберите ваш домен
3. **SSL/TLS** → **Overview**
4. Выберите **Flexible**

### 2. DNS Configuration

Добавьте A-запись:
```
Type: A
Name: @ (или subdomain)
IPv4: ваш_server_IP
Proxy status: Proxied (оранжевое облако)
```

### 3. Page Rules (Опционально)

Создайте правило для перенаправления HTTP → HTTPS:
```
URL: http://*yourdomain.com/*
Setting: Always Use HTTPS
```

### 4. Cloudflare API Token (Для автоматизации)

```bash
# В .env добавьте:
CLOUDFLARE_API_TOKEN=your_token
CLOUDFLARE_ZONE_ID=your_zone_id
```

---

## 🛠️ Обслуживание

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только приложение
docker-compose logs -f app

# Только база данных
docker-compose logs -f postgres
```

### Остановка и запуск

```bash
# Остановить все
docker-compose down

# Запустить все
docker-compose up -d

# Перезапустить конкретный сервис
docker-compose restart app
```

### Обновление приложения

```bash
# Получить последние изменения
git pull origin main

# Пересобрать и перезапустить
docker-compose up -d --build
```

### Резервное копирование БД

```bash
# Создать backup
docker-compose exec postgres pg_dump -U alley_user alley_of_heroes > backup_$(date +%Y%m%d).sql

# Восстановить backup
docker-compose exec -T postgres psql -U alley_user alley_of_heroes < backup_20240101.sql
```

### Миграции Prisma

```bash
# Применить миграции
docker-compose exec app npx prisma migrate deploy

# Сгенерировать Prisma Client
docker-compose exec app npx prisma generate
```

---

## 🚨 Устранение неполадок

### Проблема: Контейнер не запускается

```bash
# Проверить статус
docker-compose ps

# Проверить логи
docker-compose logs app

# Пересобрать без кеша
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: База данных недоступна

```bash
# Проверить здоровье PostgreSQL
docker-compose exec postgres pg_isready -U alley_user

# Перезапустить БД
docker-compose restart postgres

# Проверить логи БД
docker-compose logs postgres
```

### Проблема: 502 Bad Gateway

```bash
# Проверить, запущен ли app
docker-compose ps app

# Проверить health check
curl http://localhost:3000/api/health

# Перезапустить app
docker-compose restart app
```

### Проблема: Нет доступа к загруженным файлам

```bash
# Проверить права на uploads
docker-compose exec app ls -la /app/public/uploads

# Исправить права
docker-compose exec app chown -R nextjs:nodejs /app/public/uploads
```

### Очистка Docker

```bash
# Удалить неиспользуемые образы
docker system prune -a

# Удалить всё (ОПАСНО - удалит volume с БД!)
docker-compose down -v
```

---

## 📊 Мониторинг

### Health Check

```bash
curl http://localhost:3000/api/health
```

Ответ:
```json
{
  "status": "healthy",
  "timestamp": "2024-01-01T00:00:00.000Z",
  "database": "connected"
}
```

### Статус контейнеров

```bash
docker-compose ps
```

### Использование ресурсов

```bash
docker stats
```

---

## 🔒 Безопасность

### Чеклист перед продакшеном:

- [ ] Изменены все пароли по умолчанию
- [ ] Сгенерирован уникальный `NEXTAUTH_SECRET`
- [ ] Настроен HTTPS (через Cloudflare Flexible или Nginx)
- [ ] Настроены rate limits в Nginx
- [ ] Настроен firewall (UFW/iptables)
- [ ] Включены автоматические обновления системы
- [ ] Настроен мониторинг и алерты
- [ ] Настроено автоматическое резервное копирование БД

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте раздел [Устранение неполадок](#устранение-неполадок)
2. Просмотрите логи: `docker-compose logs -f`
3. Проверьте health check: `curl http://localhost:3000/api/health`

---

## 🎉 Готово!

После успешного развертывания:

- 🌐 **Сайт**: http://localhost:3000 (или ваш домен)
- 🔐 **Админка**: http://localhost:3000/admin
- 📊 **Health Check**: http://localhost:3000/api/health

**Не забудьте изменить пароль администратора после первого входа!**

